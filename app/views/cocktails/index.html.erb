<h1>La liste des cockatails</h1>
<div>
  <% @cocktails.each do |cocktail| %>
    <p><%= cocktail.name.capitalize %></p>
    <%= link_to "See details", cocktail, class: "btn btn-primary"%>
  <% end %>
</div>

<%= link_to "Add a new Cocktail", new_cocktail_path, class: "btn btn-warning"%>
