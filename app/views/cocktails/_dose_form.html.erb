<div style="padding:5px;">
  <p> Add an ingredient to this Cocktail !</p>
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.error_notification %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.button :submit, class:"btn btn-primary"%>
  <% end %>
</div>
